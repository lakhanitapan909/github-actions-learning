name: Environment Variable Practice

on:
  push:

env:
  APP_NAME: my-first-app

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show App Name
        run: echo "Application name is $APP_NAME"

      - name: Show Linux
        run: uname -a
